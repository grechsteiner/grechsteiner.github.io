import Layout from "../components/Layout";


type ProjectProps = {
    
};
  
export default function Project({ }: ProjectProps): React.JSX.Element {
    return (
        <Layout>
            <div>Hi</div>
        </Layout>
    );
}
