import Layout from "../components/Layout";

type WorkProps = {
    
};
  
export default function Work({ }: WorkProps) {
    return (
        <Layout>
            <div>Hi</div>
        </Layout>
    );
}
