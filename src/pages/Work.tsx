import Layout from "../components/Layout";

type WorkProps = {
    
};
  
export default function Work({ }: WorkProps): React.JSX.Element {
    return (
        <Layout>
            <div>Hi</div>
        </Layout>
    );
}
