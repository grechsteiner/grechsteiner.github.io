import Layout from "../components/Layout";

type AboutProps = {

};
  
export default function About({ }: AboutProps): React.JSX.Element {
    return (
        <Layout>
            <div>Hi</div>
        </Layout>
    );
}
